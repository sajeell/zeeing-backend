class Book {
  constructor(title, description, discountRate, coverImage, price) {
    this.title = title;
    this.description = description;
    this.discountRate = discountRate;
    this.coverImage = coverImage;
    this.price = price;
  }
}

module.exports = Book;
